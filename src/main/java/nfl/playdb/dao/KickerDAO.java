package nfl.playdb.dao;


public interface KickerDAO {

//	@Query("select k from Kicker k where gid in (select gid from Game where seas = ?1)")
//	List<Kicker> findAllForGamesInSeason(int season);

//	@Query("select k from Kicker k join Game g on k.gid = g.gid where player = ?0 and (g.seas >= ?1 and g.wk >= ?2) and (g.seas <= ?3 and g.wk <= ?4")
//	List<Kicker> findKickerDataInRange(String playerName, int startSeas, int startWk, int endSeas, int endWk);

//	@Query("select k from Kicker k join Game g on k.gid = g.gid where player = ?1 and (g.seas = ?2 and g.wk = ?3)")
//	Kicker findDataPoint(String playerName, int seas, int wk);
}
